@model IEnumerable<FabulousDB.Models.Inv_purchase_request_items>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.item.Item_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Purchase_request.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Quntaty)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.item.Item_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Purchase_request.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Quntaty)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
                @Html.ActionLink("Details", "Details", new { id=item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
